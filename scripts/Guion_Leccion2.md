## Guion Lección 3: Operadores y expresiones

Buenas tardes, en la clase de hoy vamos a ver lo que son los operadores lógicos y algunas expresiones matemáticas.

Okay. En primer lugar, las operaciones matemáticas se pueden utilizar tanto en valores enteros como en valores decimales, en floats, y también, en ciertos casos, con los strings.

Primero vamos a correr el programa... y vamos a extender esto de aquí.

Podemos ver que el entero el número 1 es 10 y el número 2 es es 5, perdón. Se puede realizar suma, resta con los símbolos tradicionales, pero también se puede hacer multiplicación con el asterisco, división con el slash y módulo o resto a través del porcentaje, el símbolo de porcentaje.

También se puede ver que en ciertos casos se puede realizar sumas de o se puede realizar operaciones con los strings. Aquí para almacenar líneas de o se puede realizar líneas de texto más complejas en variables, o que utilicen distintas variables de texto, de string, en una sola variable o en un solo en una sola en una sola depuración.

Luego iríamos con lo que son las comparaciones booleanas. Tenemos lo que es la compara estos van a devolver o un valor true o un valor false. Por ejemplo, estamos analizando si la suma y la multiplicación tienen el mismo valor con doble igual. Esto es falso, así que devuelve un false.

Estamos viendo si la suma es igual a 15, esto es true, esto es verdadero, va a devolver un true.

Estamos viendo si este es un not, estamos revisando si la resta no es igual a 5, pero sabemos que sí es 5, se ha comprobado que sí es 5, así que va a devolver un false.

Bueno, ahora también tenemos lo compara lo la comparación and, la puerta lógica and. Si es que ambos son verdaderos, entonces el resultado va a ser verdadero, pero como uno de estos es falso, que ya lo hemos probado comprobado antes, va a devolver un false.

La puerta lógica or va a devolverte un true si es que al menos uno es verdadero y, ya comprobamos que al menos uno es verdadero, así que de va a devolver un true.

También existen las comparaciones de mayor que y menor que, además de la comparación de mayor o igual que y menor o igual que.
